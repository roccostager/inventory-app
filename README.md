# inventory-app
Simple application to learn working with an express backend alongisde a postgress database.

## Instructions
1. Create a `PostgreSQL` database: run `CREATE DATABASE inventory` in `psql` shell.